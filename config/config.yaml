database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: ${DB_NAME}
  user: ${DB_USER}
  password: ${DB_PASSWORD}

data_generation:
  customers: 1000
  products: 500
  transactions: 5000
  transaction_items: 15000
  start_date: "2024-01-01"
  end_date: "2024-12-31"

pipeline:
  batch_size: 1000
  log_level: INFO
  retries: 3
  timeout_seconds: 30

bi:
  tool: powerbi
